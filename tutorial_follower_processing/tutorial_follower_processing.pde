int circleX;

void setup() {
  size(640, 360);
  circleX = 50;
}

///variables need (data) type then name followed by ;
///type name ;

void draw () {
  background(50);
  noStroke();
  fill(255);
  ellipse(circleX,180,25,25);
}
